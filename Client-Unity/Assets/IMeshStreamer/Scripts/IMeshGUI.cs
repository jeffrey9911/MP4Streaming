using UnityEditor;
using UnityEngine;

/*
[CustomEditor(typeof(IMeshManager))]
public class IMeshGUI : Editor
{
    public override void OnInspectorGUI()
    {
        // Draw the default inspector
        DrawDefaultInspector();

        IMeshManager manager = (IMeshManager)target;

        // Add your custom GUI code here
        EditorGUILayout.Space();
        EditorGUILayout.LabelField("Mesh Manager Controls", EditorStyles.boldLabel);

        // Example of a custom button
        if (GUILayout.Button("Manage Meshes"))
        {
            // Assuming you have a method to manage meshes
            Debug.Log("Managing meshes...");
        }

        // Include more controls as needed
    }
}
*/